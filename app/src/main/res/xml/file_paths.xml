<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Calibration CSV lives in getExternalFilesDir(null); share it via FileProvider. -->
    <external-files-path name="external_files" path="." />
</paths>
